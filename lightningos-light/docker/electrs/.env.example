# Copy to `.env` and fill in from /data/bitcoin/bitcoin.conf
# (rpcuser / rpcpassword lines).
BITCOIN_RPC_USER=lightningos
BITCOIN_RPC_PASS=replace-me

# Pin the electrs release built by the Dockerfile.
ELECTRS_VERSION=v0.11.1
